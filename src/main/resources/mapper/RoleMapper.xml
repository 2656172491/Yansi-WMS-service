<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.yansispringboot.mapper.RoleMapper">

    <resultMap id="RoleResultMap" type="org.example.yansispringboot.pojo.Role">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="level" property="level"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="RoleResultMap">
        SELECT * FROM roles WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="RoleResultMap">
        SELECT * FROM roles ORDER BY level ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (role_name, level, create_time, update_time)
        VALUES (#{roleName}, #{level}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE roles
        SET
            role_name = #{roleName},
            level = #{level},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM roles WHERE id = #{id}
    </delete>

</mapper>
